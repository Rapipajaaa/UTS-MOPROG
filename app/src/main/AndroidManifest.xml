<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/bl"
        android:supportsRtl="true"
        android:theme="@style/Theme._2312500602_Rafif_UTSB"
        tools:targetApi="31">
        <activity
            android:name=".Staff"
            android:exported="false" />
        <activity
            android:name=".Dosen"
            android:exported="false" />
        <activity
            android:name=".Mahasiswa"
            android:exported="false" />
        <activity
            android:name=".PerpusActivity"
            android:exported="false" />
        <activity
            android:name=".AkademikActivity"
            android:exported="false" /> <!-- Set SplashActivity as the launcher activity -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Define MainActivity without the intent filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>